package com.tcs.abn.recipeapi;

import org.springframework.context.annotation.Configuration;
/*import org.springframework.http.HttpMethod;

import org.springframework.security.config.annotation.authentication.builders.AuthenticationManagerBuilder;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.WebSecurityConfigurerAdapter;*/

@Configuration
public class SpringSecurityConfig /* extends WebSecurityConfigurerAdapter */
{

	/*
	 * @Override protected void configure(AuthenticationManagerBuilder auth) throws
	 * Exception {
	 * 
	 * auth.inMemoryAuthentication()
	 * .withUser("user").password("{noop}password").roles("USER");
	 * 
	 * }
	 * 
	 * @Override protected void configure(HttpSecurity http) throws Exception {
	 * 
	 * http.httpBasic() .and() .authorizeRequests() .antMatchers(HttpMethod.GET,
	 * "/rest/recipe/**").hasRole("USER")
	 * .antMatchers(HttpMethod.POST,"/rest/addrecipe/").hasRole("USER")
	 * .antMatchers(HttpMethod.PUT,"/rest/updateRecipe/").hasRole("USER")
	 * .antMatchers(HttpMethod.DELETE,"/rest/deleteRecipe/**").hasRole("USER")
	 * .and() .csrf().disable() .formLogin().disable(); }
	 */
	 
	

}
